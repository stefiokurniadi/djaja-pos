services:
  db:
    image: postgres:16
    container_name: pos-saas-db
    environment:
      POSTGRES_USER: pos
      POSTGRES_PASSWORD: pos
      POSTGRES_DB: pos
    ports:
      - "5432:5432"
    volumes:
      - pos_saas_pgdata:/var/lib/postgresql/data

volumes:
  pos_saas_pgdata:

